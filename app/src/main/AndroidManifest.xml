<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.your.package.name"> <!-- Ensure your package name is here -->

    <application
        android:hasCode="true"
        android:allowBackup="false"
        android:label="AI Behavior Lab"> <!-- Added label for better compatibility -->

        <meta-data
            android:name="xposedmodule"
            android:value="true" />

        <meta-data
            android:name="xposeddescription"
            android:value="AI Behavior Lab" />

        <meta-data
            android:name="xposedminversion"
            android:value="93" />

    </application>

</manifest>
